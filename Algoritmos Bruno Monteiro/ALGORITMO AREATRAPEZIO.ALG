Algoritmo "Algoritmo areatrapezio"

Var
 bsmaior, bsmenor, altura, area: real

Inicio
     escreva ("insira o valor do comprimento da bsmaior")
     leia (bsmaior)
     escreva ("insira o valor do comprimento da bsmenor")
     leia (bsmenor)
     escreva ("insira o valor da altura")
     leia (altura)
     Area <- (bsmaior + bsmenor)/2*altura
     escreva (area)

Fimalgoritmo